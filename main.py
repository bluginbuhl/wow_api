#!/usr/bin/env/ python

import requests
import json
import secrets
import oauth_token
from character import Character

TOKEN = oauth_token.getToken(secrets.CLIENT_ID, secrets.CLIENT_SECRET)


# character name, realm list

# create Character for each

# compile Character summaries into one table

# render the table